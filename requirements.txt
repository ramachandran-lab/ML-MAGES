# Recommended: Python >= 3.10 (for pandas-plink) and Python == 3.9 (for optional requirement chiscore)

# --- core numerics (upper bound needed for pandas-plink) ---
numpy>=1.22.4,<2.0        # pandas needs >=1.22.4 
pandas>=2.2,<3.0          # pandas-plink expects pandas 2.2.x–2.x 
scipy>=1.10               # satisfies chiscore + scikit-learn needs

# --- genetics / IO ---
pandas-plink>=2.3.2       # install name; import is pandas_plink 

# --- ML / stats ---
scikit-learn>=1.3.2
statsmodels>=0.14.0
torch>=2.0

# --- plotting ---
matplotlib>=3.8
seaborn>=0.13.2

